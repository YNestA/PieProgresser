function pieRoll($pie,angle) {
    var $progress=$pie.find("span.progress");
    if($progress.length){
        console.log(1);
        console.log($progress.css("transform","rotate(60deg)"));
    }
}
$(document).ready(function () {
});
